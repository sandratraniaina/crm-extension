<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lead <%= leadExpenses.lead.name %> Expenses</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto p-6">
        <h1 class="text-2xl font-bold text-gray-800 mb-6">Expenses for <%= leadExpenses.lead.name %>
        </h1>
        <table class="w-full bg-white rounded-lg shadow-lg">
            <thead>
                <tr class="bg-gray-200">
                    <th class="p-3 text-left">ID</th>
                    <th class="p-3 text-left">Amount ($)</th>
                    <th class="p-3 text-left">Description</th>
                    <th class="p-3 text-left">Date</th>
                    <th class="p-3 text-left">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% leadExpenses.expenses.forEach(exp=> { %>
                    <tr>
                        <td class="p-3">
                            <%= exp.id %>
                        </td>
                        <td class="p-3">
                            <%= exp.amount %>
                        </td>
                        <td class="p-3">
                            <%= exp.description %>
                        </td>
                        <td class="p-3">
                            <%= exp.expenseDate %>
                        </td>
                        <td class="p-3">
                            <button class="text-blue-600 hover:underline">Edit</button>
                            <button class="text-red-600 hover:underline">Delete</button>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
        <a href="/" class="mt-4 inline-block text-blue-600 hover:underline">Back to Dashboard</a>
    </div>
</body>

</html>