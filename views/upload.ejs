<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Threshold</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body class="bg-gray-50 min-h-screen flex">
    <%- include('partials/sidebar') %>

    <div class="flex-1 flex flex-col">
        <%- include('partials/header') %>

        <main class="flex-1 p-6 ml-64">
            <h1 class="text-2xl font-semibold text-gray-900 mb-6">Upload customer data</h1>
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
                <form action="/csv/upload" method="POST" class="space-y-4" enctype="multipart/form-data">
                    <div>
                        <label for="value" class="block text-sm font-medium text-gray-600 mb-1">Upload exported file</label>
                        <input type="file" id="csvfile" name="csvfile"
                            class="w-full p-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm"
                            required>
                    </div>
                    <button type="submit"
                        class="bg-blue-500 text-white p-2 rounded-lg hover:bg-blue-600 transition duration-200 text-sm">
                        Upload file
                    </button>
                </form>
            </div>
        </main>
    </div>
</body>

</html>